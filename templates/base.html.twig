<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Dragon Data CMS</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        {{ encore_entry_link_tags('app') }}
        
        <style>
            @font-face {
                font-family: 'Arcadian';
                src: url('/fonts/arcadian_ruins_regular.ttf') format('truetype');
                font-weight: normal;
                font-style: normal;
            }
        </style>

        <script src="https://cdn.tailwindcss.com"></script>

        <script>
            tailwind.config = {
                theme: {
                    extend: {
                        fontFamily: {
                            dragon: ['Arcadian', 'serif'],
                            sans: ['ui-sans-serif', 'system-ui', 'sans-serif'],
                        },
                        colors: {
                            viking: {
                                dark: '#1c1917',
                                rock: '#292524',
                                light: '#a8a29e',
                                red: '#7f1d1d',
                                fire: '#dc2626',
                                gold: '#d97706',
                                parchment: '#e7e5e4'
                            }
                        },
                        backgroundImage: {
                            'stone-pattern': "url('/images/fond_pierre.png')",
                        }
                    }
                }
            }
        </script>
    </head>
    
    <body class="bg-viking-dark bg-stone-pattern bg-repeat text-viking-parchment font-sans min-h-screen">
        
        <div id="react-root">
            <div class="flex items-center justify-center h-screen">
                <div class="text-center">
                    <h1 class="text-4xl font-dragon text-viking-gold mb-4">Chargement de l'Antre...</h1>
                    <div class="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-viking-fire mx-auto"></div>
                </div>
            </div>
        </div>

        <!-- 👇 TEST DE DIAGNOSTIC 👇 -->
        <script>
            console.log("✅ 1. Le HTML est chargé.");
            // On vérifie si les scripts Webpack sont détectés
            window.addEventListener('load', () => {
                const scripts = document.querySelectorAll('script[src*="/build/"]');
                if (scripts.length > 0) {
                    console.log("✅ 2. Le script Webpack est présent dans le HTML :", scripts[0].src);
                } else {
                    console.error("❌ 2. ERREUR : Aucun script Webpack (/build/...) trouvé dans le HTML !");
                }
            });
        </script>

        {{ encore_entry_script_tags('app') }}
    </body>
</html>